app.identity.mock.session-ttl=PT8H
app.identity.mock.citizen-password=${MOCK_CITIZEN_PASSWORD}
app.identity.mock.agent-password=${MOCK_AGENT_PASSWORD}
app.identity.mock.area-responsible-password=${MOCK_AREA_RESPONSIBLE_PASSWORD}
app.identity.mock.admin-password=${MOCK_ADMIN_PASSWORD}

# Story 3.4 / DDA2-61: habilita el endpoint interno de simulacion de
# updateTicketStatus SOLO cuando se solicita explicitamente. El default seguro
# mantiene el simulador apagado aun bajo el profile dev.
app.simulator.enabled=${SIMULATOR_ENABLED:false}
