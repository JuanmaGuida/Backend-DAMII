spring.application.name=backend

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# Story 3.3 / DDA2-59: identidad del productor en el envelope de eventos
# (Eventos v1.6 s4). Por defecto en el codigo ya son estos mismos valores
# (M2 / help-center-api); se declaran aca explicitamente para que sean
# visibles y configurables por entorno sin tocar codigo.
app.events.producer.module-id=M2
app.events.producer.service=help-center-api

# Story 3.4 / DDA2-61: el simulador de updateTicketStatus esta apagado por
# defecto. Solo se prende en application-dev.properties (o el profile que
# corresponda para QA), nunca aca.
app.simulator.enabled=false
