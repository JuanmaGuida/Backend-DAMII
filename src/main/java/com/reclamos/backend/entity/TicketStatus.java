package com.reclamos.backend.entity;

public enum TicketStatus {
    REGISTERED,
    IN_REVIEW,
    ROUTED,
    IN_PROGRESS,
    PENDING_INFORMATION,
    DUPLICATE,
    RESOLVED,
    CLOSED,
    CANCELLED
}
